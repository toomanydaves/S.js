/**
 * @module ui
 */
define([ 'jquery', 'ls/ui' ], function ( $, ui ) {
    /**
     * NavbarSubmenu is a UI compontent for embedding a sublevel, drop-down menu within a navbar menu hierarchy.
     * @class NavbarSubmenu
     * @namespace ui
     * @constructor
     * @param {Object} $el A jQuery object containing an element to use as a container for the submenu
     * @param {Object} data A JavaScript object with a name property holding an identifier string and optionally a click
     * property containing a URL string or function, and a children property containing an array of objects for creating
     * nested submenus
     * @param {Object} [options] An object to use to override the default settings
     */
    ui.NavbarSubmenu = function ( $el, data, options ) {

    };
});
