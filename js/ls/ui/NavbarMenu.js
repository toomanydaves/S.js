/**
 * @module ui
 */
define([ 'jquery', 'ls/ui', 'ls/ui/NavbarSubmenu' ], function ( $, ui, Submenu ) {
    /**
     * NavbarMenu is a UI component for embedding a top-level, drop-down menu into a navbar component.
     * @class NavbarMenu
     * @namespace ui
     * @constructor
     * @param {Object} $el A jQuery object containing an element to use as a container for the menu
     * @param {Object} data A JavaScript object with a name property holding an identifier string and optionally a click
     * property containing a URL string or function, and a children property containing an array of objects for creating
     * submenus
     * @param {Object} [options] An object to use to override the default settings
     */
    ui.NavbarMenu = function ( $el, data, options ) {

    };
});
